# System Diagram — Bind-Time Authority Fabric

Intent → Pre-State → Bind Boundary → Standing Evaluation → Decision

If Standing == TRUE → EXECUTE → Receipt → Merkle Seal

If Standing == FALSE → REFUSE → No Bind

State evolves continuously.
Bind decision is discrete.
